[match:declarations_tuple]
M1: """[uri: Uri, diagnostics: readonly Diagnostic[]]"""

[match:arg_list]
M1: """uri: Uri, diagnostics: readonly Diagnostic[]"""

[match:declarations_block]
M1: """{uri: Uri, diagnostics: readonly Diagnostic[]}"""

# [match:index_signature]
# M1: """[uri: Uri, diagnostics: readonly Diagnostic[]]"""

[match:interface]
M1: """export interface DiagnosticCollection extends Iterable<{uri: Uri, diagnostics: readonly Diagnostic[]}> {
    	readonly name: string;
    }"""
M2: """export interface DiagnosticCollection extends Iterable<[uri: Uri, diagnostics: readonly Diagnostic[]]> {
    	readonly name: { vorname: string; nachname: string; };
    }"""